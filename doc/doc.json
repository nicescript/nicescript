{"types":{"Anything":{"title":"Anything"},"Nothing":{"title":"Nothing"},"Undefined":{"title":"Undefined"},"Null":{"title":"Null"},"NotFound":{"title":"NotFound"},"Fail":{"title":"Fail"},"NeedComputing":{"title":"NeedComputing"},"Pending":{"title":"Pending"},"Stop":{"title":"Stop"},"Something":{"title":"Something"},"Ok":{"title":"Ok"},"Value":{"title":"Value"},"Object":{"title":"Object","extends":"Value"},"Box":{"title":"Box"},"Error":{"title":"Error"},"Single":{"title":"Single","extends":"Value"},"Array":{"title":"Array","extends":"Value"},"Number":{"title":"Number","extends":"Value"},"String":{"title":"String","extends":"Value"},"Boolean":{"title":"Boolean","extends":"Value"},"Interface":{"title":"Interface","extends":"Value"},"Range":{"title":"Range","extends":"Value"},"Tag":{"title":"Tag","extends":"Value"},"Div":{"title":"Div","extends":"Value"},"I":{"title":"I","extends":"Value"},"B":{"title":"B","extends":"Value"},"Span":{"title":"Span","extends":"Value"},"H1":{"title":"H1","extends":"Value"},"H2":{"title":"H2","extends":"Value"},"H3":{"title":"H3","extends":"Value"},"H4":{"title":"H4","extends":"Value"},"H5":{"title":"H5","extends":"Value"},"H6":{"title":"H6","extends":"Value"},"P":{"title":"P","extends":"Value"},"LI":{"title":"LI","extends":"Value"},"UL":{"title":"UL","extends":"Value"},"OL":{"title":"OL","extends":"Value"},"A":{"title":"A","extends":"Value"},"Img":{"title":"Img","extends":"Value"},"Input":{"title":"Input","extends":"Value"},"Button":{"title":"Button","extends":"Value"},"Textarea":{"title":"Textarea","extends":"Value"},"Submit":{"title":"Submit","extends":"Value"},"Checkbox":{"title":"Checkbox","extends":"Value"}},"functions":[{"name":"equal","source":"(a, b) => a === b || (a && a.getResult ? a.getResult() : a) === (b && b.getResult ? b.getResult() : b)","signature":[],"type":"Check","description":"Checks if two values are equal."},{"name":"true","source":"v => v === true","signature":[],"type":"Check"},{"name":"false","source":"v => v === false","signature":[],"type":"Check"},{"name":"any","source":"(v, ...vs) => vs.includes(v)","signature":[],"type":"Check"},{"name":"array","source":"a => Array.isArray(a)","signature":[],"type":"Check"},{"name":"NaN","source":"n => Number.isNaN(n)","signature":[],"type":"Check"},{"name":"object","source":"i => i !== null && typeof i === 'object' && !i._isSingleton","signature":[],"type":"Check"},{"name":"null","source":"i => i === null","signature":[],"type":"Check"},{"name":"undefined","source":"i => i === undefined","signature":[],"type":"Check"},{"name":"nice","source":"v => nice.Anything.proto.isPrototypeOf(v)","signature":[],"type":"Check"},{"name":"primitive","source":"i => {\n    const type = typeof i;\n    return i === null || (type !== \"object\" && type !== \"function\");\n  }","signature":[],"type":"Check"},{"name":"empty","source":"v => {\n    if(!v)\n      return true;\n    if(Array.isArray(v))\n      return !v.length;\n    if(typeof v === 'object')\n      return !Object.keys(v).length;\n    return !v;\n  }","signature":[],"type":"Check"},{"name":"subType","source":"(a, b) => {\n    is.string(a) && (a = nice.Type(a));\n    is.string(b) && (b = nice.Type(b));\n    return a === b || b.isPrototypeOf(a);\n  }","signature":[],"type":"Check"},{"name":"browser","source":"() => nice.isEnvBrowser","signature":[],"type":"Check"},{"name":"js","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"string","source":"v => typeof v === i","signature":[],"type":"Check"},{"name":"boolean","source":"v => typeof v === i","signature":[],"type":"Check"},{"name":"number","source":"v => typeof v === i","signature":[],"type":"Check"},{"name":"symbol","source":"v => typeof v === i","signature":[],"type":"Check"},{"name":"function","source":"v => typeof v === i","signature":[],"type":"Check"},{"name":"date","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"regExp","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"error","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"arrayBuffer","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"dataView","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"map","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"weakMap","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"set","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"weakSet","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"promise","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"evalError","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"rangeError","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"referenceError","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"syntaxError","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"typeError","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"uriError","source":"v => v && v.constructor && v.constructor.name === nice.jsTypes[i].jsName","signature":[],"type":"Check"},{"name":"Anything","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Type","source":"v => nice.Anything.isPrototypeOf(v)","signature":[],"type":"Check"},{"name":"Nothing","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Undefined","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Null","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"NotFound","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Fail","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"NeedComputing","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Pending","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Stop","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Something","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Ok","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Value","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Object","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"itemsType","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Action"},{"name":"has","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Mapping"},{"name":"get","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Mapping"},{"name":"set","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Action"},{"name":"each","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Nothing",null]},{"name":"each","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"]},{"name":"reverseEach","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"]},{"name":"assign","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Action"},{"name":"remove","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Action"},{"name":"removeAll","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Action"},{"name":"reduce","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Mapping"},{"name":"mapToArray","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Mapping"},{"name":"map","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Nothing",null],"type":"Mapping"},{"name":"map","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Mapping"},{"name":"filter","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Mapping"},{"name":"sum","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Mapping"},{"name":"some","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Check"},{"name":"every","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Check"},{"name":"find","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Mapping"},{"name":"findKey","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object"],"type":"Mapping"},{"name":"count","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Object",null],"type":"Mapping"},{"name":"Box","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"listen","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Box",null]},{"name":"listenOnce","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Box",null]},{"name":"listenDiff","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Box"]},{"name":"unsubscribe","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Box"]},{"name":"bind","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Box","Box"]},{"name":"unbind","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Box","Box"]},{"name":"Error","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Single","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Array","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"push","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"],"type":"Action"},{"name":"reduce","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array",null],"type":"Mapping"},{"name":"reduceRight","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array",null],"type":"Mapping"},{"name":"concat","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array","Array"],"type":"Mapping"},{"name":"sum","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"],"type":"Mapping"},{"name":"unshift","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"],"type":"Action"},{"name":"add","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"],"type":"Action"},{"name":"pull","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"],"type":"Action"},{"name":"insertAt","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"],"type":"Action"},{"name":"removeAt","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"],"type":"Action"},{"name":"callEach","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"]},{"name":"splice","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"],"type":"Action"},{"name":"each","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array",null]},{"name":"forEach","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array",null]},{"name":"eachRight","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array",null]},{"name":"fill","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"],"type":"Action"},{"name":"map","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array",null],"type":"Mapping"},{"name":"filter","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array",null],"type":"Mapping"},{"name":"sortBy","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"],"type":"Mapping"},{"source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Array"]},{"name":"Number","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"between","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Check"},{"name":"integer","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Check"},{"name":"saveInteger","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Check"},{"name":"finite","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Check"},{"name":"lt","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Check"},{"name":"lte","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Check"},{"name":"gt","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Check"},{"name":"gte","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Check"},{"name":"sum","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping"},{"name":"difference","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping"},{"name":"product","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping"},{"name":"fraction","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping"},{"name":"reminder","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping"},{"name":"acos","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.acos"},{"name":"asin","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.asin"},{"name":"atan","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.atan"},{"name":"ceil","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.ceil"},{"name":"clz32","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.clz32"},{"name":"floor","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.floor"},{"name":"fround","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.fround"},{"name":"imul","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.imul"},{"name":"max","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.max"},{"name":"min","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.min"},{"name":"round","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.round"},{"name":"sqrt","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.sqrt"},{"name":"trunc","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.trunc"},{"name":"abs","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.abs"},{"name":"exp","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.exp"},{"name":"log","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.log"},{"name":"atan2","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.atan2"},{"name":"pow","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.pow"},{"name":"sign","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.sign"},{"name":"asinh","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.asinh"},{"name":"acosh","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.acosh"},{"name":"atanh","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.atanh"},{"name":"hypot","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.hypot"},{"name":"cbrt","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.cbrt"},{"name":"cos","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.cos"},{"name":"sin","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.sin"},{"name":"tan","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.tan"},{"name":"sinh","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.sinh"},{"name":"cosh","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.cosh"},{"name":"tanh","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.tanh"},{"name":"log10","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.log10"},{"name":"log2","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.log2"},{"name":"log1pexpm1","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping","description":"Delegates to Math.log1pexpm1"},{"name":"clamp","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping"},{"name":"inc","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Action"},{"name":"dec","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Action"},{"name":"divide","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Action"},{"name":"multiply","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Action"},{"name":"negate","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Action"},{"name":"setMax","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Action"},{"name":"setMin","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Action"},{"name":"String","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"endsWith","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Check"},{"name":"startsWith","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Check"},{"name":"includes","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Check"},{"name":"match","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Check"},{"name":"trimLeft","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"trimRight","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"trim","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"truncate","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"capitalize","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"deCapitalize","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"toLocaleLowerCase","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"toLocaleUpperCase","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"toLowerCase","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"toUpperCase","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"charAt","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"charCodeAt","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"codePointAt","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"concat","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"indexOf","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"lastIndexOf","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"normalize","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"padEnd","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"padStart","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"repeat","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"substr","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"substring","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"slice","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"split","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"match","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"search","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"replace","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"localeCompare","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"],"type":"Mapping"},{"name":"fromCharCode","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping"},{"name":"fromCodePoint","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number"],"type":"Mapping"},{"source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["String"]},{"name":"Boolean","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"and","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Boolean"],"type":"Mapping"},{"name":"or","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Boolean"],"type":"Mapping"},{"name":"nor","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Boolean"],"type":"Mapping"},{"name":"xor","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Boolean"],"type":"Mapping"},{"name":"turnOn","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Boolean"],"type":"Action"},{"name":"turnOff","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Boolean"],"type":"Action"},{"name":"Interface","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Range","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"each","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Range"]},{"name":"map","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Range"],"type":"Mapping"},{"name":"toArray","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Range"],"type":"Mapping"},{"name":"includes","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Range"],"type":"Check"},{"name":"within","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Number","Range"]},{"name":"Tag","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"on","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Tag"],"type":"Action","description":"Adds event handler to an element."},{"name":"scrollTo","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Tag"]},{"name":"focus","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Tag"],"type":"Action"},{"name":"add","source":"(...a) => {\n    const l = types.length;\n    for(let i = 0; i < l; i++){\n      const isNice = a[i] && a[i]._isAnything;\n      const needJs = types[i].type.jsType;\n      if(needJs && isNice){\n        a[i] = a[i].getResult();\n      } else if(!needJs && !isNice){\n        a[i] = nice.toItem(a[i]);\n      }\n    }\n    return action(...a);\n  }","signature":["Tag"],"type":"Action"},{"name":"Div","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"I","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"B","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Span","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"H1","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"H2","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"H3","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"H4","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"H5","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"H6","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"P","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"LI","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"UL","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"OL","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"A","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Img","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Input","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Button","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Textarea","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Submit","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"},{"name":"Checkbox","source":"v =>\n    v && v._type ? type.proto.isPrototypeOf(v) : false","signature":[],"type":"Check"}]}